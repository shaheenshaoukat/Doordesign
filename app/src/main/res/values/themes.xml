<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Door_DEsign" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/mycll</item>
        <item name="android:navigationBarColor">@color/mycll</item>
    </style>

    <style name="Theme.Door_DEsign" parent="Base.Theme.Door_DEsign" />
</resources>